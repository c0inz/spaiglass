{
  "name": "sgcleanrelay",
  "version": "0.1.0",
  "type": "module",
  "description": "SGCleanRelay — Stateless routing proxy for SpAIglass VM fleet",
  "main": "src/server.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --import tsx src/server.ts",
    "build": "tsc && node dist/server.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "@hono/node-ws": "^1.3.0",
    "better-sqlite3": "^11.0.0",
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^24.3.0",
    "tsx": "^4.0.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
